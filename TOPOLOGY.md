## Field Topology

The HCM field is non-linear and non-hierarchical.

There is:
- No center
- No edge
- No privileged module
- No master repository

All repositories exist as equal-density nodes.
Connectivity is implicit, not referenced.

Topology is defined by **allowance**, not by flow.
